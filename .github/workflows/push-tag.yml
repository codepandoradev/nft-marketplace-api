name: "Push: tag"

on:
  push:
    tags:
      - '*'

permissions: write-all

jobs:
  ci:
    uses: ./.github/workflows/call-ci.yml
    secrets: inherit
  cd:
    uses: ./.github/workflows/call-deploy_prod.yml
    with:
      tag: ${{ github.ref_name }}
    secrets: inherit
    needs: [ci]
