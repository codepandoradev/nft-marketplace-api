name: Push tag

on:
  push:
    tags:
      - '*'

jobs:
  ci:
    uses: ./.github/workflows/ci.yml
  cd:
    uses: ./.github/workflows/cd-prod.yml
    with:
      tag: ${{ github.ref_name }}
    needs: [ci]
